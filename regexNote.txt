For structure like
<span>
rú<br />
如
</span>


use
<(br)[^>]*>(?<span>[^<]*)
to remove <br>

use html minifier to make it one line

use 
<[^>]*>
in vscode
for space replacement

use $ to replace line end 
use ^ to replace line start

https://textcleaner.net/   add {n}" to \n", and trim space
https://www.freeformatter.com/regex-tester.html  use ^ to add ", then use $ to add {n}"

redesign the media component, using state instead prop
get current active tabbar
determine the media whenever tab active changes
in sub chapters, hit play will fast forward /rewind to the exact position in audio


eas build
MUST remove gitignore for upload